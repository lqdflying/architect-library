# Shared agent assets

Optional fragments for custom agents in the Architect Skill library:

- Output templates referenced from `agents/<name>/INSTRUCTIONS.md`
- Sample hook scripts (install hooks only with explicit user opt-in)

Agent source of truth lives in `agents/<name>/` (not here).
